import styled from "styled-components";
import { fontsSizes } from "../../styles/theme";

export const TitleContainer = styled.h2`
  font-size: ${fontsSizes.big};
  font-weight: 600;
  letter-spacing: 2.5px;
`;